{
  "$schema": "https://developer.dynatrace.com/docs-assets/schema_strict_apps.json",
  "dynatrace": "1",
  "schemaId": "overview-cache",
  "version": "1.0.0",
  "displayName": "Capacity & Utilization: overview cache",
  "description": "Shared, last-write-wins snapshot of each module's Overview counts, written by module pages so the Overview loads without re-querying. Stored environment-wide as JSON split across text chunks.",
  "multiObject": false,
  "maturity": "GENERAL_AVAILABILITY",
  "ownerBasedAccessControl": false,
  "properties": {
    "chunks": {
      "displayName": "Cache (JSON chunks)",
      "description": "Serialized JSON payload split into segments of at most 4000 characters.",
      "type": "list",
      "items": {
        "type": "text",
        "constraints": [
          {
            "type": "LENGTH",
            "maxLength": 5000
          }
        ]
      },
      "nullable": false,
      "maxObjects": 100
    }
  }
}

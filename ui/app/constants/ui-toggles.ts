// Toolbar button toggles: true = show the optional label/element, false = hide.
// Icons always show.

/** Show the Strato prev/next stepper around the timeframe button. Off because
 *  the overlay already covers it. */
export const TIMEFRAME_SHOW_STEPPER = false;

/** Show the selected scope name next to the container icon. */
export const SCOPE_SHOW_LABEL = true;

/** Show the word "Filter" next to the filter icon. */
export const FILTER_SHOW_LABEL = true;

/** Show the words "Export CSV" next to the download icon. */
export const EXPORT_SHOW_LABEL = true;

/** Show the word "Columns" next to the columns icon. */
export const COLUMNS_SHOW_LABEL = true;

/** Show the word "Settings" next to the settings (gear) icon. */
export const SETTINGS_SHOW_LABEL = true;

/** Unused. The live snooze gate is `appSettings.snoozeEnabled`. */
export const SNOOZE_ENABLED = true;
